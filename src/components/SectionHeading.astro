---
type Props = {
  title: string;
  subtitle?: string;
  id?: string;
};

const { title, subtitle, id } = Astro.props;
---

<div class="space-y-5">
  <h2 id={id} class="section-title">
    {title}
  </h2>
  {subtitle && <p class="body-copy max-w-[52rem]">{subtitle}</p>}
</div>
